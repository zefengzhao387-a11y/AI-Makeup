# ═══════════════════════════════════════════════════════════
# Lumina Beauty AI 
# ═══════════════════════════════════════════════════════════

# ── Web 框架 ──────────────────────────────────────────────
fastapi>=0.115.0,<0.120.0
uvicorn>=0.30.0,<0.35.0
mangum>=0.17.0                # Vercel / Lambda ASGI 适配器

# ── 数据库 ────────────────────────────────────────────────
sqlalchemy>=2.0.36,<2.1
aiosqlite>=0.20.0             # SQLite 异步驱动（本地用）
asyncpg>=0.29.0               # Postgres 异步驱动（Vercel / Prisma Postgres）

# ── 配置 ──────────────────────────────────────────────────
pydantic>=2.9,<3
pydantic-settings>=2.5
python-dotenv>=1.0.1

# ── 安全 ──────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0    # JWT
bcrypt>=4.1.0,<5                     # 直接调用 bcrypt，避开 passlib 在 Py3.13 的坑

# ── HTTP 客户端（模块一 Stability AI 调用）────────────────
httpx>=0.27.0,<0.29
